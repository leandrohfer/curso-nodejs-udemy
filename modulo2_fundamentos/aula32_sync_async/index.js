/**
 * Em Node.js existem duas opções ao executar métodos: sync e async.
 * 
 * Basicamente o sync (Forma síncrona) é a forma comum e mais difundida de se
 * utilizar o node. Trata-se de quando o código espera ser totalmente executado
 * para prosseguir a execução. Execução sincronizada.
 * 
 * Já o async (Forma assíncrona) seria o seu inverso, trata-se de um código que 
 * continua progredindo e sendo executado até que em um ponto futuro ele obtenha
 * a resposta da execução assíncrona. Ocorre quando por exemplo é possível execu-
 * tar a segunda linha sem impedir a execução da terceira linha de código.
 */
